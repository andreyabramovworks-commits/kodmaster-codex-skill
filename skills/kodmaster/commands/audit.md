# `/audit` — legacy alias

Скрытый совместимый алиас `/kodaudit` в режиме **Discovery**.

Новые ответы и документация должны рекомендовать `/kodaudit`. Не трактовать `/audit` как Acceptance и не удалять этот файл, чтобы старые prompts продолжали работать.
